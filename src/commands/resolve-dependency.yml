description: >
  Resolve Maven dependencies.
parameters:
  command:
    type: string
    description: Dependency resolve command.
    default: mvn dependency:resolve
  attach:
    type: boolean
    description: Attach the workspace.
    default: true
  persist:
    type: boolean
    description: Persist command to workspace.
    default: true
  mentions:
    type: string
    description: Comma separated list of Slack User ID, without spaces.
    default: ""
  fail_only:
    type: boolean
    default: true
    description: Notify only for Failed job.
steps:
  - attach-workspace:
      attach: << parameters.attach >>
  - run:
      name: Resolve Dependencies
      command: << parameters.command >>
  - persist-workspace:
      persist: << parameters.persist >>
  - notify:
      mentions: << parameters.mentions >>
      fail_only: << parameters.fail_only >>
